# Home_Sales
- In this challenge, the knowledge of SparkSQL is used to determine key metrics about home sales data. Then the Spark is used to create temporary views, partition the data, cache and uncache a temporary table, and verify that the table has been uncached.